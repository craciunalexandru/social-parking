import * as React from 'react';
import MapContainer from '../Map';
import CreateEventSectionContainer from '../CreateEventSection';


const Application = () => (
    <React.Fragment>
        <MapContainer />
        <CreateEventSectionContainer />
    </React.Fragment>
);

export default Application;